# Prefer Bun but allow npm as fallback
engine-strict=false

# npm registry configuration
registry=https://registry.npmjs.org/

# Optional: Enable exact versions for better reproducibility
save-exact=true
